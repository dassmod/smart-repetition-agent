# Worklog

One dated line per work session, newest on top: where the code stopped, the single next move, any open confusion. This exists because commits record what finished, not what was mid-thought; the next session reads the top line and starts moving in seconds. Purely technical, about the code's state, nothing else.

Format: `YYYY-MM-DD: stopped at X. Next: Y. Open: Z.`

---

- 2026-08-11: card-identity rework started, ingestion end only. New file `agent/src/course_parser/lesson_cards.py` holds `PROMPT_HEADING` and `extract_prompt_texts(lesson_text)`, a flag-driven line walk that opens at `## Retrieval prompts`, collects numbered items via `re.match(r"\d+\.\s+(.*)")`, and breaks at the next `## `. Verified against the real vault: 10 prompts from 2 files, 31 files return `[]` and raise nothing. `make_card_id` is half written by hand; line 1 (sha256 of the utf-8 encoded prompt, hexdigest, first 8 chars) is correct, line 2 is not yet the f-string form. `extract_cards` not started, nothing written to disk, no card has ever been persisted. Identity decision made and recorded: content addressing, hash of the prompt text, chosen because prompts get reordered and not reworded; the matching vault editing rule is in Audit Flags. Next: fix `make_card_id` line 2 to `f"{prefix}-{text_hash}"`, and swap the prefix from raw `path.stem` to `make_lesson_id(path.stem)` from `agent/src/scheduler/review.py`, which already slugifies and is currently duplicated in spirit by the raw stem (real ids today carry spaces and a comma and are about to become JSON keys). Then `extract_cards(path)` returning dicts of id, prompt, `str(path)`, heading. Open: whether to normalize whitespace and case before hashing. Free to decide now, expensive once `review_state.json` holds cards, so decide before the first write. Also still open from 2026-08-10 and untouched: `cmd_review` line 92 writes `Rating.Again` for a tooling failure, and the six false Agains are still on disk; the rework retires dead-source cards so the empty-content branch stops firing, but the API-failure and `score` default paths keep the same defect and `ReviewSession` still has no way to advance without rating. **Priority change, later the same day, after a parallel assessment session:** the next build hour does **not** continue here. It starts Proof-of-Agent-Run rung 1, the pure-Python trace replay engine on hardcoded fixtures per `Field.md` M2. The card brick above is **parked, not abandoned**, and it is two moves from working, both written out above. The false-`Again` fix is parked on a trigger rather than a slot: do it before the first real card is ever persisted, because that is the moment it can start writing lies about a card that means something. Reason for the change: the repo's own history shows May and June 2026 at zero commits during the heaviest note-writing months, and today's build hour reproduced the same pattern live, so tooling no longer goes in front of the artifact. `Practice Loop.md` was edited the same day to match, and its first-sessions list no longer holds the flagship behind a gate.

- 2026-08-10: `load_lesson_content` rewritten for a flat folder; `VAULT_COURSES_PATH` renamed `VAULT_PATH` and repointed at `03 Life/Learning/Fields/Decentralized AI/Lessons`. No existence guard added: a missing vault crashes loud, chosen deliberately over a silent empty string. Found that `courses.json` is a cached parse and not a pointer, which is why boot never failed on the dead path. `review` now runs end to end and all six drawn cards return empty content, because the 22 stored cards name a course deleted on 2026-08-05 and no filename can match. Next: card identity per the STRATA engine rework, one card per lesson retrieval prompt, storing source path and heading; archive the old 22 rather than migrate. Open: `cmd_review` line 92 submits `Rating.Again` when a lesson file is missing and `save_review_state` persists it, so a tooling failure is written as a memory failure; the summary then reports "Reviewed: 6" with "Duration: 0.0s". Six cards already carry a false Again from this run.

- 2026-08-09: repo designated the current build artifact; STRATA.md spec and engine rework written; no code touched yet. Next: get the agent running locally (Practice Loop step 1), expect the parser to fail on the retired Courses/ path. Open: whether review_state.json loads at all with courses.json pointing at a dead path, check before touching the parser.
